version: '3.8'

services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app  # Pour le hot reload en dev
      - ./data:/app/data  # Persistance SQLite
    environment:
      - DATABASE_URL=sqlite:///./data/green_guard.db
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/ping"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s